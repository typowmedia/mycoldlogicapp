const styles = theme => ({
  root: {
    flex: 1,
    height: '100%',
    padding: theme.spacing.unit * 4,
  },
  formContainer: {
    textAlign: 'center',
  },
  formTitle: {
    paddingTop: theme.spacing.unit * 5,
  },
});

export default styles;
