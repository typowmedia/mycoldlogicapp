const styles = theme => ({
  bestSiteContainer: {
    minHeight: '100vh',
    paddingTop: theme.spacing.unit * 6,
  },
  bestSiteTitle: {
    padding: '40px 20px',
  },
  bestSiteContent: {},
});

export default styles;
